{% extends 'base.html.twig' %}

{% block title %}Hello Home page !{% endblock %}

{% block body %}


    <article>

        <h2>{{article.title}}</h2>
        <div class="metadata">{{article.createdeAt |date('d/m/Y')}} à {{article.createdeAt |date('H:i')}}</div>
        <div class="content">
            <img src="{{article.image}}" alt="">
            <p>{{article.content | raw }}</p>

        </div>
    </article>
{% endblock %}